'use strict';

describe('Controller: HomeController', function () {
    /*
    beforeEach(module('followforfollow'));

    var HomeController, scope, window;

    beforeEach(inject(function ($controller, $rootScope, _$window_) {
        window = _$window_;

        scope = $rootScope.$new();
        HomeController = $controller('HomeController', {
            $scope: scope,
            $window : window
        });
    }));

    it('should redirect me to instagram api after click on login button', function () {
        element(by.css('.btn')).click();
        expect(window.location.href).toBe("https://instagram.com/oauth/authorize/?client_id=419658066049438eb0bbd3cd7d726b9a&redirect_uri=http://followforfollow.dev/token.html&response_type=token&scope=likes+comments+relationships+basic");
    });
    */
});
